#include <stdio.h>
// this programme replicate him self
/* hello world ! */
int main() {char *s="#include <stdio.h>%c// this programme replicate him self%c/* hello world ! */%cint main() {char *s=%c%s%c;printf(s, 10, 10, 10, 34, s, 34, 10);return (0);}%c";printf(s, 10, 10, 10, 34, s, 34, 10);return (0);}
