#include <stdio.h>
// hello programme return

int hello(){int i = 32 + 8;return (i);}
int main() {/* hello world ! */
char *s="#include <stdio.h>%c// this programme replicate him self%c/* hello world ! */%cint main() {/* hello world ! */%cchar *s=%c%s%c;printf(s, 10, 10, 10, 34, s, 34, 10);return (0);}%c";printf(s, 10, 10, 10, 34, s, 34, 10);return (0);}
